{{#form}}
<form action="{{action}}" method="post">
  <fieldset class="form">
    <legend class="form-legend">Edit Candidate</legend>
    {{{fields}}}
    <div class="form-controls">
      <input type="submit" value="Save">
    </div>
  </fieldset>
</form>
{{/form}}
